<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/layout_custom_sign_in"
        android:layout_width="match_parent"
        android:layout_height="@dimen/sign_in_button_height"
        android:background="@drawable/sign_in_button_background"
        android:backgroundTint="@color/yellow">

        <TextView
            android:id="@+id/tv_custom_sign_in"
            style="@style/medium_18sp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:drawablePadding="@dimen/spacing_14dp"
            app:drawableStartCompat="@drawable/ic_kakao_simbol"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="카카오로 시작하기" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>